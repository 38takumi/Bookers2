<!--ユーザーインフォとnew bookここから-->
<%= render 'books/list', book: @book,user: @user %>
<!--ユーザーインフォとnew bookここまで-->


<!--Title用-->
<main>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
    </thead>
  
    <% @books.each do |book| %>
      <tbody>
        <tr>
          <th>
           <%= attachment_image_tag @user, :profile_image_id, :fill, 60, 60, fallback: "no_image.jpg"%><br>
           <%= link_to @book.title,user_path(@user.id) %>
          </th>
          <td><%= link_to book.title,book_path(@book.id) %></td>
          <td><%= book.body %></td>
          <!--<td><%#= link_to "Show",show_path(@book.id) %></td>-->
          <!--<td><%#= link_to "Edit",edit_path(@book.id) %></td>-->
          <!--<td><%#= link_to destroy_path(@book), "Destroy", method: :delete, data: {confirm: "Are you sure?"}%></td>-->
        </tr>
      </tbody>
    <% end %>
    </thead>
  </table>
</main>