<body>
    
  <h1>Editing Book</h1>
     
     <!--成功時のメッセージ-->
  <% if flash[:notice] %>
    <p id="notice">
      <%= flash[:notice] %>
    </p>
  <% end %>
  
  <!--エラー時のメッセージ-->
   <% if @book.errors.any? %>
       <div id="error_explanation">
         <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
         <ul>
           <% @book.errors.full_messages.each do |message| %>
             <li><%= message %></li>
           <% end %>
         </ul>
       </div>
   <% end %>
 
 <p>
   <div>
       <strong>Title</strong>
   </div>
   <textarea class="book_title" name="book[title]" id="book_title"><%= @book.title %></textarea>
   <!--<p><%= @book.title %></p>-->
 </p>
 
 <p>
   <div>
       <strong>Body</strong>
   </div>
   <textarea class="book_body" name="book[body]" id="book_body"><%= @book.body %></textarea>
   <!--<p><%= @book.body %></p>-->
 </p>
 <div class="actions">
     <!--<input type="submit" name="commit" value="Update Book" data-disable-with="Update Book">-->
     <%= link_to "Update Book",book_path(@book.id) %>
 </div>
 
 <!--<a class="show_7001" href="/books/7001">Show</a> |-->
 <!-- <a class="book_id" href="/books/id"> -->
     <%= link_to "Show",book_path(@book.id) %>
 <!-- </a> -->
  |
 <!-- <a class="back" href="/books">Back</a> -->
 <%= link_to "Back",books_path(@book.id) %>
 
   
 
 </body>
